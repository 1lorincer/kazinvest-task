{
  "name": "kazinvest-task",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build-only",
    "start": "cd server && npm install --omit=dev && node server.js"
  }
}